{
  "name": "@gsmsdev12/packages",
  "version": "0.0.0",
  "type": "module",
  "description": "GSMS shared packages",
  "author": "GuruShots Team",
  "private": false,
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "publish": "lerna publish --force-publish"
  },
  "dependencies": {
    "lerna": "^8.1.8"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
